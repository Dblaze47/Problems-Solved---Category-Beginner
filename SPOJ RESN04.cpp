#include<iostream>
#include<cstdio>
using namespace std;
#define sf scanf
#define pf printf
#define loop(x,y) for(x = 0; x<y; x++)

int main() {
    int T, N = 0;
    sf("%d", &T);
    while(T){
        int i, st, al = 0, bb = 0;
        sf("%d", &N);
        const int sz = N;
        int pilest[sz];
        for(i = 1; i<N; ++i){
            sf("%d", &st);

        }
        --T;
    }
    return 0;
}
